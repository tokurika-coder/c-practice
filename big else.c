//wap to find the bigger no between two no
#include<stdio.h>
int main()
{
	int a,b;
	printf("enter the value of a and b");
	scanf("%d%d",&a,&b);
	if(a>b)
	printf("%d is bigger",a);
	else
	printf("%d is bigger",b);
	getch();
}
